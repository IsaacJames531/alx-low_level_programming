#include <stdio.h>

/**
  * main - Prints number for base 10
  *
  * Return: Always (Success)
  */
int main(void)
{
	char a;

	for (a = '0'; a < '10'; a++)
	{
		putchar(a)
	}
	
	putchar('\n');

	return (0);
}
