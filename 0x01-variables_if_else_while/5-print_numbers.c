#include <stdio.h>

/**
  * main - Prints number for base 10
  *
  * Return: Always (Success)
  */
int main(void)
{
	int a;

	for (a = '0'; c <= '10'; a++)
	{
		print ("the number of %d is %d and is greater than 5\n", a)
	}

	return (0);
}
